version: 2.1
orbs:
  win: circleci/windows@5.0
  hugo: circleci/hugo@1.3.0
jobs:
  create-cli-windows:
    executor: win/default
    steps:
      - checkout
      - run:
          name: test hi and hi2
          shell: bash
          command: |
            echo "hi"
            echo "hi2"

workflows:
  lint-and-test:
    jobs:
      - create-cli-windows