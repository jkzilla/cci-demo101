version: 2.1

orbs: 
  slack: circleci/slack@4.5.1

jobs:
  test-install-py:
    docker:
      - image: "cimg/ruby:2.7.2-node"
    resource_class: small
    environment:
      TZ: "Etc/UTC"
    steps:
      - checkout
      - run: 
          command: |
            echo ${TZ}
      - slack/notify:
          event: pass
          template: basic_success_1
            
workflows:
  py-workflow:
    jobs:
      - test-install-py:
          context:
            - slack-orb