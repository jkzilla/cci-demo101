version: 2.1

jobs:
  cli:
    docker:
      - image: cimg/go:1.18-node
    steps:
      - run:
          name: test CLI
          command: |
            which circleci
      - checkout
      - run:
          name: bash env across steps
          command: |
            echo "i'm a change to be one ahead of staging"
      - run:
          name: bash env across steps
          command: |
            echo "i'm a change to be one ahead of staging"
workflows:
  main:
    jobs:
      - cli

# VS Code Extension Version: 1.0.0